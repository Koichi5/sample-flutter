import 'package:flutter/material.dart';

class GeminiSample extends StatelessWidget {
  const GeminiSample({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gemini Sample'),
      ),
    );
  }
}
